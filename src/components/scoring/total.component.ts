import { Component } from '@angular/core';
import { PointSheet } from './points-sheet.component';


import * as _ from "lodash";
@Component({
    selector: 'scsc-total',
    templateUrl: 'total.html',

})
export class Total extends PointSheet {
}